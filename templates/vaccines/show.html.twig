{% extends 'base.html.twig' %}

{% block title %}Vaccines{% endblock %}

{% block body %}
    <h1>Vaccines</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaccine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vaccine.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vaccines_index') }}">back to list</a>

    <a href="{{ path('app_vaccines_edit', {'id': vaccine.id}) }}">edit</a>

    {{ include('vaccines/_delete_form.html.twig') }}
{% endblock %}
