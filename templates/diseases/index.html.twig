{% extends 'base.html.twig' %}

{% block title %}Diseases index{% endblock %}

{% block body %}
    <h1>Diseases index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disease in diseases %}
            <tr>
                <td>{{ disease.id }}</td>
                <td>{{ disease.name }}</td>
                <td>
                    <a href="{{ path('app_diseases_show', {'id': disease.id}) }}">show</a>
                    <a href="{{ path('app_diseases_edit', {'id': disease.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_diseases_new') }}">Create new</a>
{% endblock %}
