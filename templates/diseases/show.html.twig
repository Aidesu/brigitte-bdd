{% extends 'base.html.twig' %}

{% block title %}Diseases{% endblock %}

{% block body %}
    <h1>Diseases</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disease.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ disease.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_diseases_index') }}">back to list</a>

    <a href="{{ path('app_diseases_edit', {'id': disease.id}) }}">edit</a>

    {{ include('diseases/_delete_form.html.twig') }}
{% endblock %}
