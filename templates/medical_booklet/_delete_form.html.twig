<form method="post" action="{{ path('app_medical_booklet_delete', {'id': medical_booklet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medical_booklet.id) }}">
    <button class="btn">Delete</button>
</form>
