{% extends 'base.html.twig' %}

{% block title %}Familiy{% endblock %}

{% block body %}
    <h1>Familiy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ familiy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ familiy.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_familiy_index') }}">back to list</a>

    <a href="{{ path('app_familiy_edit', {'id': familiy.id}) }}">edit</a>

    {{ include('familiy/_delete_form.html.twig') }}
{% endblock %}
