<form method="post" action="{{ path('app_aisle_delete', {'id': aisle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aisle.id) }}">
    <button class="btn">Delete</button>
</form>
