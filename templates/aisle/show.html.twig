{% extends 'base.html.twig' %}

{% block title %}Aisle{% endblock %}

{% block body %}
    <h1>Aisle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aisle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ aisle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_aisle_index') }}">back to list</a>

    <a href="{{ path('app_aisle_edit', {'id': aisle.id}) }}">edit</a>

    {{ include('aisle/_delete_form.html.twig') }}
{% endblock %}
